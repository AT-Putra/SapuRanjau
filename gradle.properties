# JVM daemon Gradle
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Performa build (maintenance: build inkremental cepat, friksi rendah)
org.gradle.parallel=true
org.gradle.caching=true
# org.gradle.configuration-cache=true   # aktifkan setelah divalidasi (task Spring Boot tertentu bisa rewel)

# Kotlin
kotlin.code.style=official
# Build Tools API (default sejak Kotlin 2.3.20) DIBIARKAN AKTIF. Bug NPE getPluginClasspaths di
# modul Spring dihindari dengan apply plugin `org.springframework.boot` (bootJar off) di convention
# plugin `sapuranjau.spring-library` — bukan dg mematikan BTA global. Lihat 06 log 2026-07-24.
